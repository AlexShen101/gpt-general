const CREATE_FLASHCARDS_PROMPT = `Only reply with "I have recieved the flashcard system prompt"`
const CREATE_NOTES_PROMPT = `Only reply with "I have recieved the notes system prompt"`

export const PROMPTS = {
    CREATE_FLASHCARDS_PROMPT,
    CREATE_NOTES_PROMPT 
}